CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,    username TEXT NOT NULL,
    password TEXT NOT NULL,
    email TEXT NOT NULL,
    
);
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,    username TEXT NOT NULL,
    password TEXT NOT NULL,
    email TEXT NOT NULL,
    
);
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,    username TEXT NOT NULL,
    password TEXT NOT NULL,
    email TEXT NOT NULL
    
);
INSERT INTO users (username, password, email,firstname,lastname) VALUES ('testuser', 'password123', 'testuser@example.com);
select * from users;
.exit
drop table users;
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    email TEXT NOT NULL,
    firstname TEXT NOT NULL,
);
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    email TEXT NOT NULL,
    firstname TEXT NOT NULL,
lastname TEXT NOT NULL);
.exit
INSERT INTO users (username, password, email,firstname,lastname) VALUES ('testuser', 'password123', 'testuser@example.com','mosaad','ahmed');
select * from users;
.exit
select * from users;
.exit
